<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @client.name %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @client.phone %>
</p>

<p>
  <strong>Email:</strong>
  <%= @client.email %>
</p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Peso max</th>
      <th>Altura max</th>
    </tr>
  </thead>

  <tbody>
    <% @pets.each do |pet| %>
    <tr>
      <td><%= pet.name %></td>
      <td><%= pet.max_weight %></td>
      <td><%= pet.max_height %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', clients_path %>
